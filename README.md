[README.md](https://github.com/user-attachments/files/32248186/README.md)
# jpdferreira.com site update

Replace your current root `index.html` with the provided file.

Optional SEO files:
- `robots.txt`
- `sitemap.xml`

The page is self-contained apart from Tailwind CDN and Google Fonts.

Before publishing:
1. Confirm the public contact email in `index.html`.
2. If you later add article pages, update `sitemap.xml`.
3. When you have a social preview image, add an `og:image` meta tag.
